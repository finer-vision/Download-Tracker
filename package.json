{
  "name": "fv-analytics",
  "version": "0.0.4",
  "description": "Track clicks of download links via Google Analytics",
  "main": "build/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx babel src -d build",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "download",
    "tracker"
  ],
  "author": "Finer Vision",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.2.3",
    "@babel/core": "^7.3.4",
    "@babel/preset-env": "^7.3.4",
    "@babel/register": "^7.0.0"
  }
}
